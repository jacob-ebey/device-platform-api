export default function monitorGateway(socket, user, { gatewayId }) {
  // TODO: Implement monitoring of gateway via iot stream

  // eslint-disable-next-line no-console
  console.log(`Monitoring of ${gatewayId} requested by ${user.username}`);
}
